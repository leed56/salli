import { PlaceholderScreen } from "@/components/ui/PlaceholderScreen";

export default function StockScreen() {
  return (
    <PlaceholderScreen
      title="Stock"
      description="Product list, low-stock flags, barcode lookup, sell prices, and owner-only cost price fields."
    />
  );
}
