import { PlaceholderScreen } from "@/components/ui/PlaceholderScreen";

export default function StaffSettingsScreen() {
  return (
    <PlaceholderScreen
      title="Staff"
      description="Owner-only staff accounts, roles, and access permissions will live here."
    />
  );
}
