import { PlaceholderScreen } from "@/components/ui/PlaceholderScreen";

export default function SellScreen() {
  return (
    <PlaceholderScreen
      title="New Sale"
      description="Fast product search, tap-to-add cart, cash checkout, credit checkout, invoice sharing, stock deduction, and output VAT recording."
    />
  );
}
