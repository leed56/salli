import { PlaceholderScreen } from "@/components/ui/PlaceholderScreen";

export default function OnboardingScreen() {
  return (
    <PlaceholderScreen
      title="Shop setup"
      description="Owner setup will collect shop name, VAT number, quarter start month, language, and default subscription state."
    />
  );
}
